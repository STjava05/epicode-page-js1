che cose un algorithmo:
e una strategia per arrivare da un problema a una soluzione, ho un problema applico l'algorithmo arriva la solution
probleme: voglio una camomilla , ricetta della camomilla : l'algorithmo  solution: camomilla pronto

undifined:che il variabile po essere dechiarato ma non ancora assegnato ad un valore specifico
null:é invece un assegnazione di valore che significa " nesun valore" 

un variabile é un spazio di memoria con nome utilizzata per salvare dati
 
